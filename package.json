{
  "name": "cropmate",
  "version": "1.0.0",
  "private": true,
  "description": "CropMate — smart farming platform (frontend + backend monorepo, Vercel-deployable)",
  "scripts": {
    "dev:frontend": "npm --prefix cropmate-frontend run dev",
    "dev:backend": "npm --prefix cropmate-backend run dev",
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "build": "npm --prefix cropmate-frontend run build",
    "install:all": "npm --prefix cropmate-frontend install && npm --prefix cropmate-backend install",
    "seed": "npm --prefix cropmate-backend run seed"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=18"
  }
}
